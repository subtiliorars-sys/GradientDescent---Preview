# GradientDescent — Preview

Roguelike deckbuilder x incremental x auto-battle crawler. Descend through the layers — your deck is the model, the horde is the dataset.

This repo carries sanitized snapshots only — code and design internals live in the
private HQ repo.
